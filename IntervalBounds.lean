/-
Root module for `IntervalBounds` — T4 (interval bound propagation soundness).

Grounds the Difficulty Profile (`unstable_frac`, `ibp_relative_gap`) AND supplies
the `(l,u)`-validity hypothesis of the exact-MILP oracle in `ExactMILP`.

See `IntervalBounds/README.md` and prose/ibp-relaxation-barrier-linear-regions.md.
-/

import IntervalBounds.Basic
